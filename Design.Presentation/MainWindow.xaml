<Window x:Class="Design.Presentation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Design.Presentation"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <StackPanel>
        <!--Menu Bar-->
        <DockPanel>
            <Menu DockPanel.Dock="Top">
                <MenuItem Header="_File">
                    <MenuItem Header="_Open"/>
                    <MenuItem Header="_Close"/>
                    <MenuItem Header="_Save"/>
                </MenuItem>
                <MenuItem Header="_Edit"/>
                <MenuItem Header="_View"/>
                <MenuItem Header="_Draw"/>
                <MenuItem Header="_Help"/>
            </Menu>
        </DockPanel>
        <!--Tab controller-->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                <Button Name="btn_editBeam" Content="Edit Beam" Margin="10 10 10 0" Click="Btn_editBeam_Click"/>
                <Button Content="Beam Editor" Margin="10 10 10 0"/>
                <Button Content="Beam Editor" Margin="10 10 10 0"/>
                <Button Name="btn_loadCases" Content="Load Cases" Margin="10 10 10 0" Click="btn_loadCases_Click"/>
                <Button Content="Beam Editor" Margin="10 10 10 0"/>
                <Button Content="Beam Editor" Margin="10 10 10 0"/>
                <Button Name="btn_analyse" Content="Analyse" Margin="10 10 10 0" Click="btn_analyse_Click"/>
            </StackPanel>
            <TabControl Grid.Column="1" >
                <TabItem Header="Untitled">
                    <Grid Background="#FFE5E5E5"/>
                </TabItem>
                <TabItem Header="Design-Untitled">
                    <Grid Background="#FFE5E5E5"/>
                </TabItem>
            </TabControl>
        </Grid>
    </StackPanel>
</Window>
